<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@id/nav_matches">

    <fragment
        android:id="@+id/nav_matches"
        android:name="biz.ddroid.footballpredicts.ui.matches.MatchesTabHostFragment"
        android:label="@string/menu_matches"
        tools:layout="@layout/fragment_matches_tab_host" />
    <fragment
        android:id="@+id/nav_results"
        android:name="biz.ddroid.footballpredicts.ui.results.ResultsFragment"
        android:label="@string/menu_results"
        tools:layout="@layout/fragment_results" />

</navigation>